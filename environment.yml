name: pyheatdemand
channels:
  - conda-forge
dependencies:
  - python>=3.10
  - geopandas
  - rasterstats # also installing rasterio
  - pip
  - pip:
      pyhd
      matplotlib
      tqdm
      geopy
      osmnx
